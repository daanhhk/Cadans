{
  "name": "cadans",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "lint": "biome check .",
    "format": "biome check --write .",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "node scripts/ensure-web-dist.mjs && cross-env TZ=Europe/Amsterdam vitest run",
    "build": "pnpm -r --if-present build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "cross-env": "^7.0.3",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
